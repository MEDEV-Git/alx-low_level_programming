#include "main.h"

/**
* int add: add two integer
*
* @a: first input
* @b: second input
* 
* Description: add two integer and return the result
* Return: the result success
*/

int add(int a, int b)
{
  int result = (a + b);

  return (result);
}
